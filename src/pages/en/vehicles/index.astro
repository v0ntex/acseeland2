---
import Base from '../../../layouts/Base.astro';
import View from '../../../components/views/VehiclesView.astro';
import { useTranslations } from '../../../i18n/utils';

const lang = 'en' as const;
const t = useTranslations(lang);
---
<Base title={`${t('vehicles.title')} — Auto Center Seeland AG`} description={t('vehicles.lead')}>
  <View lang={lang} />
</Base>
